from typing import Callable
import numpy as np
from PIL import Image, ImageFile

def haze(clear_img: ImageFile.ImageFile, depth_imb: ImageFile.ImageFile, A:float = 1, B:float = 1, ran_f: Callable = np.random.default_rng):
    return
